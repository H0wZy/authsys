# DSN
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=goauthdb

# JWT
# Generate with: openssl rand -base64 48
JWT_SECRET=
# JWT_ISSUER=

# Optional
# SSL_MODE=disable
# PORT=8080